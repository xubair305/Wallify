import 'package:flutter/material.dart';

class Kolors {
  Kolors._();
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const blue = Color(0xFF2962ff);
  static const appPrimary = Color(0xFF6750A4);
  static const searchBackground = Color(0xFFf5f8fd);
  static const grey = Colors.grey;
  static const green = Colors.green;
  static const red = Colors.redAccent;

  // // Delete Red color
  // static const deleteRed = Color(0xFFFF0000);
  // static Color primaryColor(BuildContext context) => context.theme.primaryColor;
  // static Color secondaryColor(BuildContext context) =>
  //     context.theme.secondaryHeaderColor;

  // // White card to black card
  // static Color cardColor(BuildContext context) => context.theme.cardColor;

  // // Change black to white color
  // static Color blackWhiteColor(BuildContext context) =>
  //     context.theme.indicatorColor;

  // // Bottom Sheet Icon Color
  // static Color? bsIconColor(BuildContext context) =>
  //     Theme.of(context).colorScheme.bottomSheetIconColor;
}
